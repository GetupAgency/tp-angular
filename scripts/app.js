/**
 * Cette variable app contient notre application angular, on lui injecte les modules et librairies externes qu'on souhaite
 * 
 * Vous remarquerez que les controlleurs démarrent par app.controller('NomDuControlleur'), on les associe donc à notre application
 */
var app = angular.module('tp-angular', ["ngRoute", "webStorageModule"]);





  